{% extends "layout.html.twig" %}

{% block content %}
    <div class="pull-right">
        <a href="{{ url('wonder/list')}}" class="btn btn-primary">Wonder List</a>
    </div>
    <fieldset>
        <legend>Wonder</legend>
        <form id="score-form" action="{{ url('wonder/save') }}" method="post">
            <input type="hidden" name="id" value="{{ data.wonder.id|default('') }}" />
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" class="form-control" id="name" name="name" value="{{ data.wonder.name|default('') }}" style="width:200px" required />
            </div>
            <div class="form-group">
                <button type="submit">Submit</button>
            </div>
        </form>
    </fieldset>
{% endblock %}
